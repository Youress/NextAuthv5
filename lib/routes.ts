export const ROOT = '/login';
export const DEFAULT_REDIRECT = '/account'; // Add a default redirect route for authenticated users
export const PUBLIC_ROUTES = ['/login', 'password-reset', 'register', 'update-password'];